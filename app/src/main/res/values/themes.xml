<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.DiscountCalcBasic" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
    <item name="colorPrimary">@color/my_light_primary</item>
        <item name="colorPrimaryDark">@color/my_light_primaryDark</item>
        <item name="android:textColorPrimary">@color/my_light_textcolor</item>

    </style>

    <style name="Theme.DiscountCalcBasic" parent="Base.Theme.DiscountCalcBasic" />
</resources>